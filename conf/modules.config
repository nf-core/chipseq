/*
 * -------------------------------------------------
 *  Nextflow config file for module specific-options
 * -------------------------------------------------
 */
params {
  modules {
    'fastqc' {
      args             = "--quiet"
      suffix           = ""
      publish_dir      = "fastqc"
      publish_results  = "all"
    }
    'trim_galore' {
      args             = "--fastqc"
      suffix           = ""
      publish_dir      = "trim_galore"
      publish_results  = "all"
    }
  }
}
