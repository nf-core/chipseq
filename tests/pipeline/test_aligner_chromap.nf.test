nextflow_pipeline {

    name "Test Workflow main.nf"
    script "main.nf"
    tag "test_aligner_chromap"
    tag "pipeline"

    test("Aligner - Chromap") {

        when {
            params {
                outdir = "$outputDir"
            }
        }

        then {
            assertAll(
                { assert workflow.success }
            )
        }

    }

}
