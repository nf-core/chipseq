nextflow_pipeline {

    name "Test Workflow main.nf"
    script "main.nf"
    tag "test_aligner_bowtie2"
    tag "pipeline"

    test("Aligner - Bowtie2") {

        when {
            params {
                outdir = "$outputDir"
                aligner = "bowtie2"
            }
        }

        then {
            assertAll(
                { assert workflow.success }
            )
        }

    }

}
